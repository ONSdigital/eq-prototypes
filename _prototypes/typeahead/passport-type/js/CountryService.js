/**
 * Country Register service singleton
 */
window.countryRegisterService = (function () {
	var baseUrl = 'https://country.register.gov.uk/records.json',
  	loadJSON(url, typeaheadData); 

  	function typeaheadData(data) {
  		data.
  	}

});